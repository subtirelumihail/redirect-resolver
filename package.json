{
  "name": "resolve-redirect",
  "version": "1.0.0",
  "description": "A Cloudflare Worker to resolve URL redirects and return the final destination",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env development",
    "deploy:production": "wrangler deploy --env production"
  },
  "keywords": ["cloudflare", "worker", "redirect", "url-resolver"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
} 